# ether.fi Cash Dashboard 配置

# 服务端口和地址
HOST=0.0.0.0
PORT=8788

# JWT 密钥 (务必修改为随机字符串)
JWT_SECRET=change-me-to-a-random-secret

# JWT Token 过期时间 (小时)
TOKEN_EXPIRE_HOURS=24

# 默认管理员账号 (首次启动自动创建)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=admin123
